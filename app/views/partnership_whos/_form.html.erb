<%= vuelidate_form_with model: @partnership, url: url, html: {class: "centered single", "data-animation-type" => "slideLeft"} do |f| %>
	<%= f.text_field :uid, required: true%>
	<%= f.submit :class => "cta" %>
<% end %>
