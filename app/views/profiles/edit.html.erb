<div class="centered-container">
	<h2><%= t(".title") %></h2>

	<%= render 'form', profile: @profile, url: partnership_profile_path %>

	<%= link_to t(".back"), edit_partnership_path(@partnership), class: "link" %>
</div>
